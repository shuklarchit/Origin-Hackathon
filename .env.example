# Server Port
PORT=8000

# SQLite Database Location
DATABASE_PATH=./data/advisor.db

# Optional LLM API Keys (Groq or Google Gemini or OpenAI)
# If no key is set, the application automatically uses the built-in
# clinical heuristic advisory engine (100% reliable, zero key required).
GROQ_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=
